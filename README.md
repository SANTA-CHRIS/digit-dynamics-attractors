# digit-dynamics-attractors
A digit-based dynamical system based on squaring each digit of an integer and concatenating the results, then (2) repeatedly compressing  the expanded sequence by summing every consecutive triplet of digits whenever the total  length remains divisible by three.
